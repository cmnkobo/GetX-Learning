class StudentDetailss {
  //Making individual variables as observable
  // var name = 'tomiwa'.obs;
  // var age = 15.obs;

  // Making entire class observable
  dynamic name;
  dynamic age;
  StudentDetailss({this.age, this.name});
}
